on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: test-coverage

permissions: read-all

jobs:
  test-coverage:
    runs-on: windows-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install dependencies
        run: |
          install.packages(c("remotes"))
          remotes::install_deps(dependencies = TRUE)
          remotes::install_cran(c("covr", "xml2"))
        shell: Rscript {0}

      - name: Test coverage
        run: |
          temp_lib <- file.path(Sys.getenv("RUNNER_TEMP"), "library")
          dir.create(temp_lib, showWarnings = FALSE, recursive = TRUE)
          .libPaths(c(temp_lib, .libPaths()))

          install_path <- file.path(normalizePath(Sys.getenv("RUNNER_TEMP")), "package")
          dir.create(install_path, showWarnings = FALSE, recursive = TRUE)

          cov <- covr::package_coverage(
            quiet = FALSE,
            clean = FALSE,
            install_path = install_path
          )
          covr::to_cobertura(cov)
        shell: Rscript {0}

      - name: Upload coverage to Codecov
        run: |
          Invoke-WebRequest -Uri "https://codecov.io/bash" -OutFile "codecov.sh"
          bash codecov.sh -f cobertura.xml -t ${{ secrets.CODECOV_TOKEN }}
        shell: pwsh

      - name: Show testthat output
        if: always()
        run: |
          Get-ChildItem -Recurse -Path "$env:RUNNER_TEMP\package" -Filter "testthat.Rout*" | ForEach-Object {
            Get-Content $_.FullName
          }
        shell: pwsh

      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-test-failures
          path: ${{ runner.temp }}/package
